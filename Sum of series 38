Write a Python program to find the sum of the series 2 +22 + 222 + 2222 + .. n terms
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,i, sum=0,temp=2;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        sum=sum+temp;
        temp=temp*10+2;
        
    }
    printf("%d",sum);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
